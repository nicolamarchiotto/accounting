# Setup script for owners, accounts and categories

Python script for setupping database table with owners, accounts and categories

Be sure to edit you setup.json accordingly before setup

Edit .json user with same credentials defined in .env with UI_USERNAME and UI_PASSWORD

Be sure to have app running

```
python setup.py
```
